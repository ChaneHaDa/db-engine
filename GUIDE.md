# DB 엔진 스터디 가이드

## 목표

이 저장소는 C++로 데이터베이스 엔진의 기초를 아래에서부터 직접 만들어 보기 위한 학습용 프로젝트입니다.

핵심 원칙은 간단합니다.

- 완성된 답안을 복붙하지 않는다
- 한 번에 한 계층씩 구현한다
- 작은 테스트로 바로 검증한다

## 추천 학습 순서

아래 순서대로 진행하는 것을 권장합니다.

1. `Page`
2. `Pager`
3. `Slotted Page`
4. `Table Heap`
5. `B-Tree`
6. `Transaction / Recovery`

## 핵심 개념

- `Page`: 보통 4096바이트 크기의 고정 길이 바이트 버퍼
- `Pager`: 메모리의 페이지를 디스크 파일과 주고받는 계층
- `Slotted Page`: 한 페이지 내부에 레코드를 어떻게 배치할지 정하는 구조
- `Table Heap`: 여러 데이터 페이지를 관리하는 계층
- `B-Tree`: 빠른 조회를 위한 인덱스 구조

처음에는 `Page`에만 집중하세요. 트리나 트랜잭션으로 너무 빨리 넘어가면 오히려 흐름이 꼬입니다.

## 1단계: Page

먼저 페이지를 메모리 안의 고정 크기 바이트 배열로 구현합니다.

권장 요구사항:

- 크기는 항상 4096바이트
- 새 페이지는 0으로 초기화된 상태로 시작
- 호출자가 raw byte에 직접 접근할 수 있어야 함
- 페이지 전체를 다시 0으로 리셋할 수 있어야 함

권장 API:

```cpp
class Page {
public:
    static constexpr std::size_t kPageSize = 4096;

    Page();

    std::uint8_t* data();
    const std::uint8_t* data() const;
    void reset();
};
```

권장 연습:

- 새 페이지가 전부 0인지 확인하기
- 몇 바이트를 직접 써보고 다시 읽기
- `reset()` 호출 후 전부 0으로 돌아가는지 확인하기

완료 기준:

- 페이지를 "고정 크기 바이트 상자"로 이해한다
- 0 초기화, 바이트 쓰기, 리셋 테스트가 모두 통과한다

## 2단계: Pager

`Page`가 익숙해지면 이제 파일과 연결합니다.

권장 요구사항:

- DB 파일을 열거나, 없으면 생성
- `page_id`에 해당하는 위치에 페이지 하나를 기록
- `page_id`에 해당하는 위치에서 페이지 하나를 읽기
- EOF 너머를 읽으면 0으로 채워진 페이지 반환
- 파일 크기를 기준으로 전체 페이지 수 계산

구현하면서 스스로 답해야 할 질문:

- `page_id`는 파일의 몇 바이트 위치로 변환되는가?
- pager는 부분 페이지를 읽고 써야 하는가, 아니면 항상 전체 페이지를 다뤄야 하는가?
- 빈 파일의 `page_count()`는 얼마여야 하는가?

완료 기준:

- `page 0`, `page 1`, `page 2`를 정상적으로 읽고 쓸 수 있다
- sparse read가 0으로 채워진 페이지를 반환한다
- 파일 크기와 페이지 수 계산이 일관된다

## 3단계: Slotted Page

파일 I/O가 되면 이제 한 페이지 내부 구조를 정의합니다.

권장 레이아웃:

```text
[Header][Slot Directory] ... free space ... [Record Bytes]
```

이 단계에서 구현할 것:

- page header
- slot metadata
- record insert
- record read
- record delete
- 선택적으로 compact

## 공부 방식

- 테스트는 작고 단순하게 유지한다
- public 함수 하나씩 구현한다
- 코드를 쓰기 전에 불변식(invariant)을 먼저 적는다
- 단계가 끝날 때마다 커밋한다

## 첫 번째 마일스톤

`Pager`로 돌아가기 전에 아래를 먼저 끝내세요.

1. `Page` 만들기
2. 0 초기화 테스트 작성
3. 바이트 쓰기 테스트 작성
4. `reset()` 테스트 작성

파일이 답안처럼 느껴지기 시작하면 다시 더 단순한 스캐폴드로 줄이세요.
